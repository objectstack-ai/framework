---
title: Project
description: Project protocol schemas
---

{/* ⚠️  AUTO-GENERATED — DO NOT EDIT. Run build-docs.ts to regenerate. Hand-written docs go in content/docs/guides/. */}

Project-Per-Database Isolation Protocol

Each **project** owns a physically isolated database. The Control Plane stores

all project metadata; project DBs contain only business data rows.

Split of concerns:

- **Control Plane**: `sys_project` (includes physical DB addressing),

`sys_package_installation` (with project_id), `sys_metadata` (with project_id),

`sys_project_credential`, `sys_project_member`.

- **Data Plane**: each project DB contains only business objects

(account, task, …). No system tables, no `project_id` columns.

<Callout type="info">
**Source:** `packages/spec/src/cloud/project.zod.ts`
</Callout>

## TypeScript Usage

```typescript
import { Project, ProjectCredential, ProjectCredentialStatus, ProjectDriver, ProjectMember, ProjectRole, ProjectStatus, ProjectType, ProvisionOrganizationRequest, ProvisionOrganizationResponse, ProvisionProjectRequest, ProvisionProjectResponse } from '@objectstack/spec/cloud';
import type { Project, ProjectCredential, ProjectCredentialStatus, ProjectDriver, ProjectMember, ProjectRole, ProjectStatus, ProjectType, ProvisionOrganizationRequest, ProvisionOrganizationResponse, ProvisionProjectRequest, ProvisionProjectResponse } from '@objectstack/spec/cloud';

// Validate data
const result = Project.parse(data);
```

---

## Project

### Properties

| Property | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| **id** | `string` | ✅ | UUID of the project (stable, never reused) |
| **organizationId** | `string` | ✅ | Organization that owns this project |
| **displayName** | `string` | ✅ | Display name shown in Studio and APIs |
| **isDefault** | `boolean` | ✅ | Whether this is the default project for the organization |
| **isSystem** | `boolean` | ✅ | Whether this is a system project (platform infrastructure, not user data) |
| **plan** | `Enum<'free' \| 'starter' \| 'pro' \| 'enterprise' \| 'custom'>` | ✅ | Plan tier for this project |
| **status** | `Enum<'provisioning' \| 'active' \| 'suspended' \| 'archived' \| 'failed' \| 'migrating'>` | ✅ | Project lifecycle status |
| **createdBy** | `string` | ✅ | User ID that created the project |
| **createdAt** | `string` | ✅ | Creation timestamp (ISO-8601) |
| **updatedAt** | `string` | ✅ | Last update timestamp (ISO-8601) |
| **databaseUrl** | `string` | optional | Full connection URL for the project database |
| **databaseDriver** | `string` | optional | Data-plane driver key (turso, libsql, sqlite, memory, postgres) |
| **storageLimitMb** | `integer` | optional | Storage quota in megabytes |
| **provisionedAt** | `string` | optional | Provisioning timestamp (ISO-8601) |
| **metadata** | `Record<string, any>` | optional | Free-form metadata |
| **hostname** | `string` | optional | Canonical hostname for this project (e.g. acme-dev.objectstack.app or api.acme.com). UNIQUE. Auto-set on creation; can be overridden for custom domains. |
| **visibility** | `Enum<'private' \| 'unlisted' \| 'public'>` | ✅ | Public exposure of this project artifacts (private | unlisted | public). |


---

## ProjectCredential

### Properties

| Property | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| **id** | `string` | ✅ | UUID of the credential |
| **projectId** | `string` | ✅ | Project this credential authorizes |
| **secretCiphertext** | `string` | ✅ | Encrypted auth token or secret (ciphertext) |
| **encryptionKeyId** | `string` | ✅ | Encryption key ID used to encrypt the secret |
| **authorization** | `Enum<'full_access' \| 'read_only'>` | ✅ | Authorization scope for this credential |
| **status** | `Enum<'active' \| 'rotating' \| 'revoked'>` | ✅ | Credential lifecycle status |
| **createdAt** | `string` | ✅ | Creation timestamp (ISO-8601) |
| **expiresAt** | `string` | optional | Optional expiry timestamp |
| **revokedAt** | `string` | optional | Revocation timestamp (if revoked) |


---

## ProjectCredentialStatus

Credential lifecycle status

### Allowed Values

* `active`
* `rotating`
* `revoked`


---


---

## ProjectMember

### Properties

| Property | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| **id** | `string` | ✅ | UUID of the membership |
| **projectId** | `string` | ✅ | Project this membership grants access to |
| **userId** | `string` | ✅ | User ID |
| **role** | `Enum<'owner' \| 'admin' \| 'maker' \| 'reader' \| 'guest'>` | ✅ | Per-project role |
| **invitedBy** | `string` | ✅ | User ID that granted this membership |
| **createdAt** | `string` | ✅ | Creation timestamp (ISO-8601) |
| **updatedAt** | `string` | ✅ | Last update timestamp (ISO-8601) |


---

## ProjectRole

Per-project role

### Allowed Values

* `owner`
* `admin`
* `maker`
* `reader`
* `guest`


---

## ProjectStatus

Project lifecycle status

### Allowed Values

* `provisioning`
* `active`
* `suspended`
* `archived`
* `failed`
* `migrating`


---

## ProjectType

Project type (prod/sandbox/dev/test/…)

### Allowed Values

* `production`
* `sandbox`
* `development`
* `test`
* `staging`
* `preview`
* `trial`


---

## ProvisionOrganizationRequest

### Properties

| Property | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| **organizationId** | `string` | ✅ | Organization being bootstrapped |
| **defaultProjectDisplayName** | `string` | ✅ | Display name for the default project |
| **driver** | `string` | optional | Driver key |
| **plan** | `Enum<'free' \| 'starter' \| 'pro' \| 'enterprise' \| 'custom'>` | optional | Plan tier |
| **storageLimitMb** | `integer` | optional | Storage quota in megabytes |
| **createdBy** | `string` | ✅ | User ID that initiated provisioning |
| **metadata** | `Record<string, any>` | optional | Free-form metadata |


---

## ProvisionOrganizationResponse

### Properties

| Property | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| **defaultProject** | `Object` | ✅ | Default project that was created |
| **durationMs** | `number` | ✅ | Total bootstrap duration in milliseconds |
| **warnings** | `string[]` | optional | Non-fatal warnings |


---

## ProvisionProjectRequest

### Properties

| Property | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| **organizationId** | `string` | ✅ | Organization that will own the new project |
| **displayName** | `string` | ✅ | Display name shown in Studio and APIs |
| **driver** | `string` | optional | Driver key (defaults to provisioning service config) |
| **plan** | `Enum<'free' \| 'starter' \| 'pro' \| 'enterprise' \| 'custom'>` | optional | Plan tier |
| **storageLimitMb** | `integer` | optional | Storage quota in megabytes |
| **isDefault** | `boolean` | optional | Mark as the organization default project |
| **createdBy** | `string` | ✅ | User ID that initiated the provisioning |
| **metadata** | `Record<string, any>` | optional | Free-form metadata |
| **hostname** | `string` | optional | Canonical hostname for this project (auto-generated if omitted) |
| **templateId** | `string` | optional | Template to seed into the project on first provisioning (e.g. "crm", "todo"). Defaults to "blank". |
| **visibility** | `Enum<'private' \| 'unlisted' \| 'public'>` | ✅ | Public exposure of this project artifacts. private = auth required for every read (default); unlisted = downloadable when commit id is known; public = listed and freely downloadable via /pub/v1/projects/:id/*. |


---

## ProvisionProjectResponse

### Properties

| Property | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| **project** | `Object` | ✅ | Provisioned project (includes database addressing) |
| **credential** | `Object` | ✅ | Freshly-minted credential for the project DB |
| **durationMs** | `number` | ✅ | Total provisioning duration in milliseconds |
| **warnings** | `string[]` | optional | Non-fatal warnings emitted during provisioning |


---

